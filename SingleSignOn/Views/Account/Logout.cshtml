@model LogoutViewModel

<div class="logout-page">
    <div class="row">
        <div class="col-sm-6">
            <p>Would you like to logout of PantryTrackers?</p>
            <form asp-action="Logout">
                <input type="hidden" name="logoutId" value="@Model.LogoutId"  />
                <fieldset>
                    <div class="form-group">
                        <button class="btn btn-action">Yes</button>
                    </div>
                </fieldset>
            </form>
        </div>
    </div>
</div>