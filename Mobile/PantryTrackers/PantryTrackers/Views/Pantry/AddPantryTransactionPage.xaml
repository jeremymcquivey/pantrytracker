<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PantryTrackers.Views.Pantry.AddPantryTransactionPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Add Inventory" 
                       HorizontalOptions="CenterAndExpand"
                       VerticalOptions="CenterAndExpand" />
                
                <Button HorizontalOptions="End" Text="Barcode"/>
            </StackLayout>
            
            <StackLayout>
                <Frame>
                    <Frame.Content>
                        <ScrollView>
                            <StackLayout>
                                <Label Text="Product Code" />
                                <Entry Placeholder="Product Code" Text="{Binding ProductCode}" />

                                <Label Text="Product Name" />
                                <Entry Placeholder="Product Name" Text="{Binding ProductName}" IsEnabled="False" />

                                <Label Text="Quantity" />
                                <Entry Placeholder="Quantity" Text="{Binding Quantity}" Keyboard="Numeric" />

                                <Label Text="Package Size" />
                                <Entry Placeholder="Package Size" Text="{Binding ContainerSize}" Keyboard="Numeric" />

                                <Label Text="Unit" />
                                <Entry Placeholder="Unit" Text="{Binding Unit}" />

                            </StackLayout>
                        </ScrollView>
                    </Frame.Content>
                </Frame>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>