<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PantryTrackers.Views.Pantry.ProductSearchPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Product Name" />
            <Entry Placeholder="Product Name" Text="{Binding SearchText}" />

            <StackLayout Orientation="Horizontal">
                <Button Text="Search" Command="{Binding ProductSearchCommand}"></Button>
                <Button Text="New" Command="{Binding NewProductCommand}" HorizontalOptions="EndAndExpand"></Button>
            </StackLayout>

            <!-- Search Results -->
            <ListView x:Name="ItemSearchResults" SelectionMode="Single" SelectedItem="{Binding SelectedProduct}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>